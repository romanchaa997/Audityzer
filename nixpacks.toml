# Nixpacks config for Railway deployment
# Ensures correct Node.js version and skip unnecessary build steps

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["npm ci --omit=dev || npm install --production"]

[start]
cmd = "node server.js"

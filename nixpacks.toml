# Nixpacks config for Railway — server.js only deploy
# Skip postinstall (fix-dependencies.js) and frontend build (vite)

[phases.install]
cmds = ["npm install --production --ignore-scripts"]

[phases.build]
cmds = ["echo 'Server-only deploy — skipping frontend build'"]

[start]
cmd = "node server.js"

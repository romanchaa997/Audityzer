apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auditorsec-platform.fullname" . }}
  labels:
    {{- include "auditorsec-platform.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}

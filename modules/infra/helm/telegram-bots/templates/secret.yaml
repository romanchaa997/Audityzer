apiVersion: v1
kind: Secret
metadata:
  name: {{ include "telegram-bots.fullname" . }}
  labels:
    {{- include "telegram-bots.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}

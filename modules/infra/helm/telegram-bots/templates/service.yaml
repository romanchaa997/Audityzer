apiVersion: v1
kind: Service
metadata:
  name: {{ include "telegram-bots.fullname" . }}
  labels:
    {{- include "telegram-bots.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telegram-bots.selectorLabels" . | nindent 4 }}

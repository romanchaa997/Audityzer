apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "telegram-bots.fullname" . }}
  labels:
    {{- include "telegram-bots.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
